import XCTest
@testable import SobelEdgeDetectionFilterPackage

final class SobelEdgeDetectionFilterPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SobelEdgeDetectionFilterPackage().text, "Hello, World!")
    }
}
